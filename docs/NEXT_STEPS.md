# Next Steps

These are follow-up items captured from hosted-server testing.

1. `PLP_fnc_clearMissionData` should pause automatic saving long enough for an admin to disconnect after clearing mission state.
2. Vehicles spawned with AI crew should not persist by default.

